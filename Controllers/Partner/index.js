// Require all Partner Controllers
const createPartner = require('./createPartner');
const loginPartner = require('./loginPartner');
const editInfo = require('./editInfo');
const uploadPartner = require('./uploadPartner');

// Export All Partner Controllers refusePartner }
module.exports = { createPartner, loginPartner, editInfo, uploadPartner }