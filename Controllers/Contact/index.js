// Require all Contact Controllers
const createContact = require('./createContact');
const createFeedback = require('./createFeedback');

// Export All Contact Controllers
module.exports = { createContact, createFeedback }